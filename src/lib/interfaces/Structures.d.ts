interface Range {
    from: number
    to: number
}

type ScreenRange = {
    height: range
    width: range
}

type Sizes = {
    innerWidth: number
    innerHeight: number
    videoWidth: number
    videoHeight: number
}