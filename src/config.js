
export const config = {
    faceMesh: {
        selfieMode: true,
        enableFaceGeometry: false,
        maxNumFaces: 2,
        refineLandmarks: false,
        minDetectionConfidence: 0.5,
        minTrackingConfidence: 0.5
    }
}